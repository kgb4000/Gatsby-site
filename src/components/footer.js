import React from 'react'
import Container from '../components/container'

export default () => (
  <footer style={{ padding: `4em 0`, backgroundColor: `red`, color: `#fff` }}>
    <Container>
      <p>Ready to book?</p>
    </Container>
  </footer>
)
